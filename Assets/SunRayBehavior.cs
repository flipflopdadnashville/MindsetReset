using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SunRayBehavior : MonoBehaviour
{
    public MazecraftGameManager instance;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
       //this.gameObject.transform.position = new Vector3(instance.activePlayer.transform.position.x, instance.activePlayer.transform.position.y + 350, instance.activePlayer.transform.position.z);
    }
}
