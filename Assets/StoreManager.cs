using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using GameBench;

public class StoreManager : MonoBehaviour
{
    //public MazecraftGameManager instance;
    //public HourlyReward hourlyReward;
    //public Text bombText;

    // Start is called before the first frame update
/*    void Start()
    {
       instance.currentBombs = 10; 
       bombText.text = instance.currentBombs.ToString();
    }

    void Update(){
        if(bombText.text != instance.currentBombs.ToString()){
            bombText.text = instance.currentBombs.ToString();
        }
    }

    public void Buy(int numberBought){
        if(PlayerData.Instance.Coins >= numberBought * 100){
            instance.currentBombs = instance.currentBombs + numberBought;
            bombText.text = instance.currentBombs.ToString();
            hourlyReward.SetCoinText(numberBought * 100);
        }
        else{
            Debug.Log("Not enough moola... go play more");
        }
    }
*/}
